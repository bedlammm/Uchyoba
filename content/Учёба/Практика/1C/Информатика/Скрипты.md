> [!example]- 1. Аргументы скрипта
> **Специальные переменные**
> 
> | Переменная | Описание | Пример |
> | :--- | :--- | :--- |
> | `$0` | Имя самого скрипта | `./script.sh` |
> | `$1`, `$2`... | Первый, второй аргумент | `start` |
> | `$#` | Количество переданных аргументов | `2` |
> | `$@` | Все аргументы одной строкой (список) | `"arg1" "arg2"` |
> | `$?` | Код возврата последней команды (0 - успех) | `0` |
> | `$$` | PID текущего процесса | `1234` |
> 
> **Шаблон проверки аргументов**
> ```bash
> if [ -z "$1" ]; then
>     echo "Usage: $0 <argument>"
>     exit 1
> fi
> ```

> [!example]- 2. Отправка в Telegram
> **Параметры**
> *   `TOKEN`: Токен от BotFather.
> *   `CHAT_ID`: ID чата (получить через getUpdates).
> *   `TEXT`: Текст сообщения.
> 
> **Команда (curl)**
> ```bash
> curl -s -X POST "https://api.telegram.org/bot<TOKEN>/sendMessage" -d chat_id="<CHAT_ID>" -d text="<TEXT>"
> ```
> 
> **Функция для вставки в скрипт**
> ```bash
> send_tg() {
>     curl -s -X POST "https://api.telegram.org/botTOKEN/sendMessage" \
>     -d chat_id="CHAT_ID" \
>     -d text="$1" > /dev/null
> }
> ```

> [!example]- 3. Условия (if / else)
> **Синтаксис**
> ```bash
> if [ условие ]; then
>     # команды
> elif [ другое_условие ]; then
>     # команды
> else
>     # команды
> fi
> ```
> 
> **Сравнение чисел**
> 
> | Флаг | Значение | Матем. аналог |
> | :--- | :--- | :--- |
> | `-eq` | Равно | `==` |
> | `-ne` | Не равно | `!=` |
> | `-gt` | Больше | `>` |
> | `-lt` | Меньше | `<` |
> | `-ge` | Больше или равно | `>=` |
> | `-le` | Меньше или равно | `<=` |
> 
> **Сравнение строк**
> 
> | Флаг | Описание |
> | :--- | :--- |
> | `= ` | Строки совпадают |
> | `!=` | Строки не совпадают |
> | `-z` | Строка пустая (Zero length) |
> | `-n` | Строка НЕ пустая (Non-zero) |
> 
> **Проверка файлов**
> 
> | Флаг | Описание |
> | :--- | :--- |
> | `-f` | Файл существует и это обычный файл |
> | `-d` | Директория существует |
> | `-e` | Объект существует (файл или папка) |
> | `-s` | Файл существует и не пустой |
> | `-r` | Доступен для чтения |
> | `-x` | Доступен для исполнения |

> [!example]- 4. Циклы
> **For (Перебор элементов/файлов)**
> ```bash
> for file in /var/log/*.log; do
>     echo "$file"
> done
> ```
> 
> **For (C-style, счетчик)**
> ```bash
> for (( i=1; i<=10; i++ )); do
>     echo "$i"
> done
> ```
> 
> **While (Чтение файла построчно)**
> ```bash
> cat file.txt | while read line; do
>     echo "$line"
> done
> ```

> [!example]- 5. Обработка текста (Grep, Awk, Cut)
> **GREP (Поиск)**
> ```bash
> grep "text" file      # Строки с "text"
> grep -v "text" file   # Строки БЕЗ "text"
> grep -r "text" dir/   # Поиск в папке рекурсивно
> grep -E "a|b" file    # ИЛИ (расширенный режим)
> ```
> 
> **AWK (Извлечение столбцов)**
> ```bash
> # $1 - первый столбец, $NF - последний столбец
> awk '{print $1}' file.txt
> 
> # Свой разделитель (-F)
> awk -F ":" '{print $1}' /etc/passwd
> ```
> 
> **WC (Подсчет)**
> ```bash
> wc -l file.txt        # Количество строк
> wc -w file.txt        # Количество слов
> ```
> 
> **SED (Замена)**
> ```bash
> sed 's/old/new/g' file.txt  # Заменить old на new везде
> ```

> [!example]- 6. Массивы (Task: Азбука Морзе)
> **Ассоциативные массивы (Словарь)**
> ```bash
> declare -A map
> map["A"]=".-"
> map["B"]="-..."
> 
> key="A"
> echo "${map[$key]}"  # Выведет .-
> ```
> 
> **Обычные массивы**
> ```bash
> arr=("Debian" "Arch" "Ubuntu")
> 
> echo "${arr[0]}"   # Первый элемент
> echo "${arr[@]}"   # Все элементы
> echo "${#arr[@]}"  # Длина массива
> ```

> [!example]- 7. Строки (Slicing)
> **Операции**
> 
> | Синтаксис | Действие |
> | :--- | :--- |
> | `${#str}` | Длина строки |
> | `${str:0:1}` | Первый символ (срез с 0 длиной 1) |
> | `${str:1}` | Все символы, начиная со 2-го |
> | `${str^^}` | Перевод в верхний регистр (UPPERCASE) |
> | `${str,,}` | Перевод в нижний регистр (lowercase) |
> 
> **Посимвольный перебор (для Морзе)**
> ```bash
> STR="HELLO"
> LEN=${#STR}
> 
> for (( i=0; i<LEN; i++ )); do
>     CHAR="${STR:$i:1}"
>     echo "$CHAR"
> done
> ```

> [!example]- 8. Математика (Task: Калькулятор)
> **Целые числа**
> ```bash
> A=10; B=5
> SUM=$(( A + B ))
> DIV=$(( A / B ))
> ```
> 
> **Дробные числа (bc)**
> ```bash
> # scale=2 (кол-во знаков после запятой)
> RES=$(echo "scale=2; 10.5 / 2" | bc)
> ```
> 
> **Case (выбор операции)**
> ```bash
> case $OP in
>     +) RES=$((A + B)) ;;
>     -) RES=$((A - B)) ;;
>     *) echo "Error" ;;
> esac
> ```

> [!example]- 9. Web (Curl) и Parsing (XML/HTML)
> **Curl — основные флаги**
> 
> | Флаг | Описание |
> | :--- | :--- |
> | `-s` | **Silent**. Не показывать прогресс-бар и мусор в консоли. |
> | `-L` | **Location**. Следовать за перенаправлениями (если сайт кидает с http на https). |
> | `-o <file>`| **Output**. Записать вывод в указанный файл. |
> | `-O` | Сохранить файл под его оригинальным именем. |
> | `-I` | **Head**. Скачать только заголовки (без тела HTML). |
> | `-w "%{http_code}"` | **Write**. Вывести конкретную инфу (обычно код ответа). |
> 
> **Сценарии использования**
> ```bash
> # 1. Получить HTML-код страницы в переменную (для парсинга)
> CONTENT=$(curl -sL "https://example.com")
> 
> # 2. Скачать файл и сохранить как data.xml
> curl -sL -o data.xml "https://cbr.ru/scripts/XML_daily.asp"
> 
> # 3. Получить ТОЛЬКО код ответа (200, 404, 503) — для мониторинга
> # -o /dev/null выбрасывает html-код, остается только то, что в -w
> CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://example.com")
> ```
> 
> **Парсинг XML (Задача "Валютчик")**
> *Вариант 1: Правильный (через xmllint)*
> ```bash
> # Извлечь курс (Value) для валюты с ID R01235 (USD)
> # "string(...)" убирает теги <Value>...</Value>
> curl -s "URL" | xmllint --xpath "string(//Valute[@ID='R01235']/Value)" -
> ```
> 
> *Вариант 2: "Костыльный" (через grep/sed)*
> ```bash
> # -A 1 : взять строку совпадения + 1 строку ПОСЛЕ неё
> curl -s "URL" | grep -A 1 "USD" | grep "Value" | sed 's/<[^>]*>//g'
> ```

> [!example]- 10. Cron и Aliases
> **Cron (Расписание)**
> Команда: `crontab -e`
> 
> | Формат | Значение |
> | :--- | :--- |
> | `* * * * *` | Мин Час День Мес День_недели |
> | `*/5 * * * *` | Каждые 5 минут |
> | `0 9 * * *` | Каждый день в 09:00 |
> | `@reboot` | При запуске системы |
> 
> **Пример строки Cron**
> ```text
> 0 9 * * * /bin/bash /home/user/script.sh
> ```
> 
> **Alias (Сокращения)**
> Файл: `~/.bashrc`
> ```bash
> alias name='command'
> ```