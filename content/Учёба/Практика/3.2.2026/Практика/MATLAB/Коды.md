# 1
```
x = -5:0.1:5;

y = 4 .* x.^2 - 2 .* x + 6;

figure; % создать холст, окно

plot(x, y, 'y-.', 'LineWidth', 2); % сделать на этом холсте график. x и y значит сделать его по аргументам x и y. y-. значит Жёлтая Штрих-пунктирная линия. Ещё бывают такие:

% Коды цветов:
% r (red), g (green), b (blue), y (yellow), k (black), w (white), c (cyan/голубой), m (magenta/пурпурный).

% Типы линий:
% - (непрерывная), -- (пунктир), : (точечная), -. (штрих-пунктир).

grid on; % это включить разлиновку графика

title('f(x) = 4x^2 - 2x + 6'); % это чтобы задать сверху название

xlabel('X');
% это лейблы которые будут висеть рядом с осями 
ylabel('Y');

```

# 2

```
I = eye(3); % это задаёт квадратную единичную матрицу 3x3

disp('Исходная единичная матрица 3x3:');
% disp просто выводит в консоль текст, как cout но без функций
disp(I);

new_row = [23, 46, 15]; % это чтобы задать новую строку матрицы. Пока она отдельна от той единичной I

A = [I; new_row]; % а вот тут их объединение. Вторая встанет снизу

disp('Результирующая матрица после добавления строки:');

disp(A);
```

# 3
```
% x^2/16 + y^2/4 - z^2 = 9
% При z = 0: x^2/16 + y^2/4 = 9
% x^2/16 + y^2/4 = 9 | : 9 => x^2/(16*9) + y^2/(4*9) = 1 => x^2/144 + y^2/36 = 1
% a^2 = 144 => a = 12
% b^2 = 36 => b = 6
% Площадь эллипса S = pi * a * b
a = sqrt(16 * 9);
b = sqrt(4 * 9);
S = pi * a * b;
% тут просто переменные задаются

fprintf('Полуоси сечения при z=0: a = %.0f, b = %.0f\n', a, b);
fprintf('Площадь сечения (z=0): S = %.4f = %d*pi\n', S, a*b);
% а вот это уже cout с функциями

figure;

f = @(x, y, z) (x.^2)/16 + (y.^2)/4 - z.^2 - 9;
% @(x, y, z) говорит что написанное далее это функция а не просто разовое вычисление, а в скобках это входные аргументы для этой функции

fimplicit3(f, [-20 20 -10 10 -10 10], 'EdgeColor', 'none'); % это для отрисовки на холсте уже трёхмерной функции. 
% 'EdgeColor', 'none' значит чтобы без сетки по всей поверхности, с ней плохо выглядит

colormap jet; % это тоже чтобы выглядело лучше, в плане цвета конкретно

shading interp; % это чтобы более гладкая поверхность была 

colorbar; % это делает легенду по цветам сбоку

camlight; % а это как бы добавляет источник света от направления камеры

lighting gouraud; % это чтобы отражение света из предыдущей команды лучше выглядело

title('x^2/16 + y^2/4 - z^2 = 9');

xlabel('Ось X');

ylabel('Ось Y');

zlabel('Ось Z');
```

# 4
```
syms m l g k h t A B phi1 phi2 real % это объявление символьных переменных (как букв в алгебре). real значит, что они не комплексные, а вещественные числа

% 1. Синфазные колебания (маятники отклоняются вместе, пружина не деформируется)
% Работает только гравитация. Уравнение: I*alpha = M_mg

omega1 = sqrt(g / l);

% 2. Противофазные колебания (маятники отклоняются в разные стороны на угол theta)
% Пружина растягивается на 2*x, где x = h*theta. Сила F = k*2*h*theta.
% Момент силы пружины: M_k = F * h = 2*k*h^2*theta.
% Уравнение: m*l^2 * theta_ddot = -m*g*l*theta - 2*k*h^2*theta

omega2 = sqrt((m*g*l + 2*k*h^2) / (m*l^2));
disp('Частота синфазных колебаний (w1):'); disp(omega1);
disp('Частота противофазных колебаний (w2):'); disp(omega2);
disp('Закон движения системы (суперпозиция нормальных мод):');
theta1 = A*cos(omega1*t + phi1) + B*cos(omega2*t + phi2);
theta2 = A*cos(omega1*t + phi1) - B*cos(omega2*t + phi2);
disp('Угол отклонения 1-го маятника:'); disp(theta1);
disp('Угол отклонения 2-го маятника:'); disp(theta2);

m_val = 1; l_val = 1; g_val = 9.81; k_val = 5; h_val = 0.5;
w1_num = sqrt(g_val / l_val);
w2_num = sqrt((m_val*g_val*l_val + 2*k_val*h_val^2) / (m_val*l_val^2));
t_num = 0:0.05:20; % вот это массив чисел от 0 до 20 с шагом 0.05

% Начальные условия: отклонили только первый маятник

A_num = 0.1; B_num = 0.1;
th1_num = A_num*cos(w1_num*t_num) + B_num*cos(w2_num*t_num);
th2_num = A_num*cos(w1_num*t_num) - B_num*cos(w2_num*t_num);
figure;
plot(t_num, th1_num, 'r-', 'LineWidth', 1.5); hold on;
plot(t_num, th2_num, 'b--', 'LineWidth', 1.5);
grid on;
title('Закон движения связанных маятников (явление биений)');
xlabel('Время t, с');
ylabel('Угол отклонения \theta, рад');
legend('Маятник 1', 'Маятник 2');
```

# 5
```
syms x_sym;
y_sym = log(cos(x_sym));

% Формула длины дуги: L = int( sqrt(1 + (dy/dx)^2) )

dy_dx = diff(y_sym, x_sym);
L_expr = sqrt(1 + dy_dx^2);

% Символьное интегрирование

L_sym = int(L_expr, x_sym, 0, pi/4);
L_val = double(L_sym);
disp('--- Результат вычисления длины дуги ---');
disp(['Аналитический вид интеграла длины: ', char(L_sym)]);
fprintf('Точное числовое значение: L = %.4f\n', L_val);
x_num = linspace(0, pi/4, 100);
y_num = log(cos(x_num));
figure;
plot(x_num, y_num, 'b-', 'LineWidth', 2);
hold on; % это чтобы было 2 графика на одном холсте

% Отметим границы интегрирования красными узлами

plot(0, log(cos(0)), 'ro', 'MarkerSize', 8, 'MarkerFaceColor', 'r');
plot(pi/4, log(cos(pi/4)), 'ro', 'MarkerSize', 8, 'MarkerFaceColor', 'r');
grid on;
title(['Длина дуги y = ln(cos x) на [0, \pi/4]. L = ', num2str(L_val, '%.3f')]);
xlabel('Ось X');
ylabel('Ось Y');
legend('Кривая', 'Границы интегрирования', 'Location', 'southwest');
```

# 6
```
A = [2, 1, 3;
4, 0, 1;
5, 2, 3];
detA = det(A);
disp('Исходная матрица A:');
disp(A);
fprintf('Определитель матрицы A: det(A) = %d\n', detA);
```

# 7
```
x = 0:0.2:2*pi;
y = sin(x + 1);
figure('Position', [100, 100, 800, 600]);
subplot(2, 2, 1); % вот это значит разбить холст на 2 строки и на 2 столбца, то есть на 4 секции, и сделать активной секцию №1.
% Нумерация секций идёт сверху вниз, слева направо, то есть 1 — верхний левый, 2 — верхний правый, 3 — нижний левый, 4 — нижний правый.. Сейчас любые команды будут применяться к левой верхней секции. 
plot(x, y, 'b-', 'LineWidth', 1.5);
grid on;
title('Стиль: plot() - Непрерывная интерполяция');
xlabel('Ось X'); ylabel('Ось Y');
subplot(2, 2, 2); % а вот тут фокус смещается на вторую секцию, то есть на правое верхнее окно.
bar(x, y, 'FaceColor', [0.2 0.6 0.5]); % эта команда чтобы отрисовать столбчатую диаграму. 
grid on;
title('Стиль: bar() - Значения в виде гистограммы');
xlabel('Ось X'); ylabel('Ось Y');
subplot(2, 2, 3);
stem(x, y, 'r', 'filled', 'LineWidth', 1.2); % эта чтобы отрисовать данные в виде дискретного графика отсчётов
grid on;
title('Стиль: stem() - Дискретные сигналы');
xlabel('Ось X'); ylabel('Ось Y');
ax = subplot(2, 2, 4, polaraxes);
polarplot(ax, x, y, 'm-', 'LineWidth', 1.5); % а это чтобы отрисовать график в полярных координатах
title('Стиль: polarplot()');
disp('ВЫВОД ПО ЗАДАЧЕ 7:');
disp('plot - оптимален для отображения непрерывных функций.');
disp('bar - удобен для статистического сравнения величин на разных интервалах.');
disp('stem - применяется в цифровой обработке сигналов для визуализации дискретных отсчетов.');
disp('polar - демонстрирует радиальное распределение амплитуды, x интерпретируется как угол.');
```

# 8
```
syms x_sym;
y_upper = x_sym; % Прямая проходит выше параболы на [0, 1]
y_lower = x_sym^2; % Парабола

% 1. Точки пересечения (границы интегрирования)

limits = solve(y_upper == y_lower, x_sym);
a = double(min(limits)); % 0
b = double(max(limits)); % 1

% 2. Формула объема тела вращения с отверстием внутри (шайбы):
% V = pi * int( R_upper^2 - r_lower^2 ) dx

V_expr = pi * (y_upper^2 - y_lower^2);
V_sym = int(V_expr, x_sym, a, b);
fprintf('Границы интегрирования: от x = %d до x = %d\n', a, b);
disp(['Точное значение объема: V = ', char(V_sym)]);
fprintf('Численное значение: V = %.4f\n', double(V_sym));
figure;

% Двумерная площадь, которая вращается

subplot(1, 2, 1);
x_num = linspace(-0.2, 1.2, 100);
plot(x_num, x_num, 'b-', 'LineWidth', 1.5); hold on;
plot(x_num, x_num.^2, 'r-', 'LineWidth', 1.5);

% Заливка области

fill([linspace(0,1,50) linspace(1,0,50)], ...
[linspace(0,1,50) linspace(1,0,50).^2], 'g', 'FaceAlpha', 0.3);
grid on;
title('Вращаемая область (2D)');
xlabel('Ось X'); ylabel('Ось Y');
legend('y = x', 'y = x^2', 'Площадь сечения');

% Трехмерная визуализация самого тела вращения

subplot(1, 2, 2);
theta = linspace(0, 2*pi, 40);
x_3d = linspace(0, 1, 40);
[T, X_grid] = meshgrid(theta, x_3d);

% Поверхность от прямой y = x (внешний конус)

R_out = X_grid;
Y_out = R_out .* cos(T);
Z_out = R_out .* sin(T);
surf(X_grid, Y_out, Z_out, 'FaceColor', 'b', 'FaceAlpha', 0.5, 'EdgeColor', 'none'); hold on;

% Поверхность от параболы y = x^2 (внутренняя воронка)

R_in = X_grid.^2;
Y_in = R_in .* cos(T);
Z_in = R_in .* sin(T);
surf(X_grid, Y_in, Z_in, 'FaceColor', 'r', 'FaceAlpha', 0.8, 'EdgeColor', 'none');
view(30, 30);
camlight; lighting gouraud;
title('Полученное тело вращения (3D)');
xlabel('Ось X'); ylabel('Ось Y'); zlabel('Ось Z');
axis equal;
```
